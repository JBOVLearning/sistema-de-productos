RewriteEngine On

# si existe archivo/carpeta, sirve normal
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# manda todo a public/
RewriteRule ^ public/index.php [L]